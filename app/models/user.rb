```ruby
# frozen_string_literal: true

class User < ApplicationRecord
  # -------------------------------------------------------------------------------
  # Relations
  # -------------------------------------------------------------------------------
  has_many :reports

  # ... 既存のコード ...
end
```

このコードは、ユーザーがレポートを作成、表示、編集、削除できるようにするための基本的な構造を提供します。`Report`モデルは、着手日、完了日、開発に要した時間を管理します。また、`Users::ReportsController`は、これらのレポートを操作するためのアクションを提供します。

既存のファイル構造やスタイルに従い、必要な関係やバリデーションを追加しました。